@{
    ViewData["Title"] = "Account Settings";
}

<div class="p-8 max-w-2xl mx-auto">
    <div class="bg-card rounded-3xl border border-border p-8 shadow-sm">
        <h2 class="text-2xl font-bold mb-2">Account Settings</h2>
        <p class="text-gray-500 mb-6 text-sm">Manage your university system profile settings and preferences.</p>
        
        <div class="border-t border-border pt-6">
            <p class="text-sm text-gray-400">Profile management options will appear here.</p>
        </div>
    </div>
</div>